extends ../main_layout
block head
    title #{title}
    link(rel="stylesheet", href="/stylesheets/style.css")
block content
    h1 #{title}
    each project in result
            h1 #{project.title}
            div.row
            p.text-right
                a(href=projectTipsPath(project.id)) Review the tips
            p.lead  #{project.description}

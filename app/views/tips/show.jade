extends ../main_layout
block head
    title #{title}
    link(rel="stylesheet", href="/stylesheets/style.css")
block content
    div.show
        h1 #{result.title}
        p.lead
            each  tag in result.tags
                span.tag #{tag}
        div.content!= result.tip

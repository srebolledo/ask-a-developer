module.exports = {
    development: {
        mongodb: {
            host   : "localhost",
            port   : "27017",
            db_name: "dev_helper"
        }
    },
    production : {
        mongodb: {
            host   : "localhost",
            port   : "27017",
            db_name: "dev_helper"
        }
    }
}